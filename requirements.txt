dotenv
# Core LangChain and Multi-Agent Workflow
langgraph>=0.6.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-google-genai>=2.1.0
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.3.0

# Vector Store and Embeddings
# chromadb>=1.0.0
sentence-transformers>=3.0.0

pip install faiss-cpu

# Enhanced Document Processing - Core Libraries
pypdf>=5.0.0
python-docx>=1.1.0
docx2txt>=0.8
pymupdf>=1.23.0  # Enhanced for metadata-based visual detection

# OCR Support (recommended for comprehensive extraction)
pdf2image>=1.16.0
pytesseract>=0.3.10
Pillow>=10.0.0

# VLM Dependencies for Batch Processing (optional but recommended)
transformers>=4.45.0
torch>=2.0.0
torchvision>=0.15.0
accelerate>=0.30.0
bitsandbytes>=0.41.0  # For memory-efficient model loading

# Enhanced Processing and Parallel Execution
# Note: threading, multiprocessing, tempfile, shutil, pathlib, contextlib, gc are built-in modules

# Utilities and Type Hints
typing-extensions>=4.8.0
# Note: uuid, re, logging, os, time are built-in modules

# Progress Tracking and Debugging
tqdm>=4.66.0
python-dotenv>=1.0.0

scipy>=1.10.0
scikit-learn>=1.2.0
requests>=2.31.0
datasets>=2.18.0
# System Requirements
# For Ubuntu/Debian systems:
# sudo apt-get install tesseract-ocr
# sudo apt-get install poppler-utils

# Performance Monitoring (optional)
# psutil>=5.9.0  # For system resource monitoring
# memory-profiler>=0.60.0  # For memory usage tracking

# Web Interface (optional - uncomment if needed)
# streamlit>=1.28.0
# gradio>=4.0.0
# fastapi>=0.104.0
# uvicorn>=0.24.0

# Advanced Analytics (optional - uncomment if needed)
# numpy>=1.24.0
# pandas>=2.0.0
# matplotlib>=3.7.0
# seaborn>=0.12.0

# Additional Model Backends (optional - choose based on your LLM preference)
# openai>=1.0.0  # For OpenAI models
# anthropic>=0.7.0  # For Claude models
# ollama>=0.1.0  # For local Ollama models

# Production Deployment (optional - uncomment if deploying)
# gunicorn>=21.2.0
# docker>=6.1.0
# kubernetes>=28.1.0
# redis>=5.0.0  # For caching and session management